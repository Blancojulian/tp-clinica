import { TimestampFechaPipe } from './timestamp-fecha.pipe';

describe('TimestampFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new TimestampFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
