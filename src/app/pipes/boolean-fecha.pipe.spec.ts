import { BooleanFechaPipe } from './boolean-fecha.pipe';

describe('BooleanFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new BooleanFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
