import { EdadFechaPipe } from './edad-fecha.pipe';

describe('EdadFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new EdadFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
