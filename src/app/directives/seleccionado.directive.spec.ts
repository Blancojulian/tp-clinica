import { SeleccionadoDirective } from './seleccionado.directive';

describe('SeleccionadoDirective', () => {
  it('should create an instance', () => {
    const directive = new SeleccionadoDirective();
    expect(directive).toBeTruthy();
  });
});
